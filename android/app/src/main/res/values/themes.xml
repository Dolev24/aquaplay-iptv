<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Black under everything, and no system bars. The page draws its own
         chrome and a video plane sits behind it; anything the platform paints
         in between shows up as a band down one side of a broadcast picture. -->
    <style name="Theme.AquaPlay" parent="android:Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
